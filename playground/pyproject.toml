[project]
name = "playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["remote-env"]

[tool.uv.sources]
remote-env = { path = "../client", editable = true }
